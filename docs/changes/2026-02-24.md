# 2026-02-24

## Commit 1 - Websocket Config Write Hardening

- Added `@websocket_api.require_admin` to `nx_displaygrid/config/set` so only HA admins can save shared `nx-displaygrid` config.
- Added backend payload sanitization/limits for websocket config writes (type checks, nesting depth, key/item counts, string length, payload size).
- Bumped the in-repo custom integration manifest version to `0.1.1` after backend behavior changes.

Why:
- Non-admin users should not be able to overwrite shared dashboard config; write access needed HA permission enforcement at the custom integration boundary.
- Payload validation reduces storage corruption risk from malformed or unexpectedly large websocket writes.

How to verify:
1. Run `python3 -m py_compile config/custom_components/nx_displaygrid/__init__.py config/custom_components/nx_displaygrid/websocket.py`.
2. Run `bash scripts/check.sh` and confirm the Node test suite still passes.
3. In Home Assistant, try saving `nx-displaygrid` config as a non-admin user and confirm the websocket write is rejected.
4. As an admin user, save a normal settings change and confirm it still persists.

Version assumptions:
- Reviewed Home Assistant developer websocket decorator docs and Home Assistant 2026.2 release notes on 2026-02-24.
- Assumes `@websocket_api.require_admin` remains the supported permission guard for custom websocket commands in the current HA Core series.

## Commit 2 - Admin PIN Hashing and Export Redaction

- Added `config/www/nx-displaygrid/util/admin-pin.util.js` to hash and verify admin PINs with a salted SHA-256 format (`sha256$<salt>$<hash>`).
- Updated admin unlock/set flows to verify hashed PINs, migrate legacy plaintext PINs after a successful unlock, and store new PINs as hashes.
- Updated the Settings unlock button handler to await async PIN verification.
- Stopped including `admin_pin` in YAML export serialization by default and added tests covering PIN hashing + export redaction.

Why:
- Plaintext admin PINs were being persisted in shared config/local storage and could leak through YAML export/copy flows.
- A compatibility-preserving migration avoids breaking existing setups while improving stored secret hygiene.

How to verify:
1. Run `node --test tests/*.test.mjs` and confirm the new admin PIN tests pass.
2. In Home Assistant, set or change the admin PIN in `nx-displaygrid` Settings and confirm admin unlock still works on this device.
3. If you have an older plaintext PIN stored, unlock once and confirm future unlocks still work (legacy value should be rewritten as a hash).
4. Use any nx-displaygrid YAML export/copy action and confirm `admin_pin:` is not included in the exported YAML.

Version assumptions:
- Reviewed Home Assistant frontend/custom-card docs and Home Assistant 2026.2 release notes on 2026-02-24; no HA API changes assumed for this client-side PIN storage hardening.

## Commit 3 - Service Version Audit Inventory Runbook

- Added `scripts/audit-service-versions.sh` to inventory running container names/images/tags/status on the media/services host and optional HA host info over SSH.
- Added floating-tag warnings (`latest`, `stable`, `nightly`, `develop`, etc.) in the audit output to highlight production pinning risks.
- Added `docs/SERVICE_VERSION_AUDIT.md` with a repeatable workflow for comparing inventory output against official stable release sources.

Why:
- This repo did not contain deployed service version information, so production-readiness audits needed a repeatable inventory step against real hosts.
- Floating tags and undocumented version drift are common causes of unstable upgrades and rollback uncertainty.

How to verify:
1. Run `bash -n scripts/audit-service-versions.sh`.
2. Run `scripts/audit-service-versions.sh --help` and confirm usage output renders.
3. Run `scripts/audit-service-versions.sh --media <user@media-host> [--ha <root@ha-host>]` against reachable hosts and confirm container/image/tag tables print.
4. Confirm floating tags are marked with `WARN(floating-tag:...)` in the `Notes` column.

Version assumptions:
- Reviewed Home Assistant release notes/docs on 2026-02-24 for the HA-specific inventory checks and backup terminology.
- Vendor stable version comparisons are intentionally manual and should use official release channels at audit time (live data changes frequently).

## Commit 4 - Lovelace Dashboard Shared Config Includes

- Added shared YAML include fragments for `nx-displaygrid` card layout, people, calendars, todos, and shopping defaults under `config/includes/`.
- Refactored both `config/lovelace/nx-displaygrid.yaml` and `config/lovelace/nx-displaygrid-v2.yaml` to reuse the shared fragments instead of duplicating the same blocks.
- Kept existing dashboard IDs, paths, titles, resources, and V2-only feature flags unchanged.

Why:
- The two dashboards duplicated the same base card config, increasing drift risk when updating people/source mappings or layout tuning.
- Shared includes reduce maintenance overhead while keeping YAML dashboard mode and file paths stable.

How to verify:
1. Run `bash scripts/check.sh` (Node tests should still pass; YAML/HA checks run if installed).
2. In Home Assistant, open `nx-displaygrid` and `nx-displaygrid v2` and confirm both dashboards still load.
3. Confirm both dashboards still show the same people, calendars, todo lists, shopping entity, and home controls.
4. Make a test edit to a shared include (for example a person label in `config/includes/nx_displaygrid_people.yaml`) and confirm it affects both dashboards after reload.

Version assumptions:
- Reviewed Home Assistant dashboard/YAML docs on 2026-02-24; this refactor assumes YAML `!include` and merge-key (`<<:`) parsing remains supported in Lovelace YAML dashboards in the current HA Core series.

## Commit 5 - Watchtower Alert Notify Service Deduplication

- Added `script.nx_sentinel_watchtower_alert_notify` in `config/packages/common/home_infra.yaml` as the single notifier-service wrapper for watchtower alerts.
- Updated nx-bastion down/restored and low-disk automations to call the new script instead of the notify service directly.
- Updated `nx_sentinel_watchtower_queue_stuck_notify` to reuse the new wrapper, leaving only one direct `notify.home_infra_alerts_*` call in the package.

Why:
- The phone notify service ID was repeated in multiple automations/scripts, making notifier renames or mobile-app re-registration more error-prone.
- Centralizing the notifier path reduces drift and makes alert routing changes safer.

How to verify:
1. Run `bash scripts/check.sh` (Node tests should still pass; YAML/HA checks run if installed).
2. In Home Assistant, trigger a watchtower alert condition (or run the scripts manually) and confirm the phone notification still arrives.
3. In Developer Tools, run `script.nx_sentinel_watchtower_alert_notify` with a test `message` field and confirm delivery.
4. Confirm `config/packages/common/home_infra.yaml` contains only one direct `notify.home_infra_alerts_*` service reference.

Version assumptions:
- Reviewed Home Assistant script/automation YAML docs on 2026-02-24; this refactor assumes script field templating in `data.message` remains supported in the current HA Core series.

## Commit 6 - Lovelace Merge-Include Compatibility Fix

- Replaced `<<: !include ../includes/nx_displaygrid_layout.yaml` with inline layout tuning keys in both `nx-displaygrid` dashboards.
- Kept the other shared Lovelace includes (`people`, `calendars`, `todos`, `shopping`) unchanged.
- Verified the fix by syncing to the HA host and running `ha core check` successfully.

Why:
- Home Assistant's Lovelace YAML loader on the target host rejected the merge-key + `!include` pattern with “expected a mapping or list of mappings for merging, but found scalar”.

How to verify:
1. Sync `config/` to the HA host (your normal `rsync` command is fine).
2. Run `ha core check` on the target and confirm it passes.
3. Reload/open `nx-displaygrid v2` and confirm the previous parse error is gone.
4. Confirm layout timing settings are unchanged (`6/22`, `30` minutes, `120` px/hour, `300000` refresh).

Version assumptions:
- Checked Home Assistant 2026.2.3 behavior on the target host on 2026-02-24; this fix assumes the current annotated YAML loader does not support `<<:` merge keys when the merged mapping is supplied via `!include` in Lovelace dashboard YAML.

## Commit 7 - V2 Settings Syntax Fix and Resource Cache-Bust

- Fixed a Lit template-string parse error in `config/www/nx-displaygrid/views/settings.view.js` by replacing the inline backtick text around `days` with HTML `<code>` markup.
- Bumped `config/lovelace/resources.yaml` `v=` query string to force clients to fetch the corrected frontend bundle.
- Verified local JS parsing and synced the update to the HA host with a passing `ha core check`.

Why:
- The V2 dashboard Settings screen failed at runtime with `SyntaxError: Unexpected identifier 'days'` because literal backticks inside a JS template string terminated the template early.

How to verify:
1. Sync `config/` to the HA host (your `rsync` command with excludes is fine).
2. Reload `nx-displaygrid v2` (hard refresh if needed) and open Settings.
3. Confirm the previous `settings.view.js ... Unexpected identifier 'days'` browser console error is gone.
4. Confirm Home Assistant `ha core check` still passes after the resource version bump.

Version assumptions:
- Reviewed Home Assistant Lovelace resource cache behavior/docs and current 2026.2.3 host behavior on 2026-02-24; cache-bust query-string versioning remains the safe way to invalidate custom-card frontend caches in this repo.

## Commit 8 - V2 Sidebar Navigation Bounce Guard

- Updated `config/www/nx-displaygrid/nx-displaygrid.adaptive.js` so the short manual-navigation adaptive lock is honored even when adaptive presentation runs in a forced tick after refresh.
- Added `tests/adaptive-navigation-lock.test.mjs` covering the regression path where a manual sidebar click would otherwise be overwritten by `force: true` adaptive screen selection.
- Bumped `config/lovelace/resources.yaml` `v=` query string to force clients to fetch the updated frontend code.

Why:
- Manual sidebar clicks in `nx-displaygrid v2` could bounce back to the schedule/recommended screen because `_refreshAll()` triggers a forced adaptive presentation tick that bypassed the initial manual-idle guard.

How to verify:
1. Run `node --test tests/adaptive-navigation-lock.test.mjs` and confirm it passes.
2. Sync `config/` to the HA host (your `rsync` command with excludes is fine).
3. Open `nx-displaygrid v2`, click a sidebar menu item (for example `Shopping` or `Settings`), and confirm it stays on that view instead of bouncing back.
4. Confirm `ha core check` still passes on the target host after syncing.

Version assumptions:
- Checked the behavior against the current host flow on 2026-02-24 (`_refreshAll()` calling `_tickV2AdaptivePresentation({ force: true })`); this fix assumes manual navigation should temporarily win over adaptive auto-screen recommendations in V2.

## Commit 9 - V2 Manual Navigation Idle Timeout Enforcement

- Updated `config/www/nx-displaygrid/nx-displaygrid.adaptive.js` so adaptive auto-screen switching respects the configured `auto_screen_idle_seconds` manual-idle window even during forced adaptive ticks.
- Tightened `tests/adaptive-navigation-lock.test.mjs` to verify forced adaptive refreshes do not override manual navigation after the short lock expires, and only switch after the adaptive idle timeout passes.
- Bumped `config/lovelace/resources.yaml` `v=` query string again to force the browser to fetch the updated frontend code.

Why:
- A periodic refresh path still triggered `force: true` adaptive ticks that could return the UI to `schedule` roughly every minute after the initial 5-second manual-nav lock expired.

How to verify:
1. Run `node --test tests/adaptive-navigation-lock.test.mjs` and confirm it passes.
2. Sync `config/` to the HA host and run `ha core check`.
3. Open `nx-displaygrid v2`, click a sidebar view (for example `Shopping`), and wait at least 60-120 seconds.
4. Confirm the screen does not bounce back before the configured adaptive idle timeout (default `180` seconds), then confirm adaptive auto-screen can resume after that timeout if enabled.

Version assumptions:
- Checked the current V2 adaptive flow on 2026-02-24 (forced adaptive ticks occur after refresh completion); this fix assumes manual navigation should remain authoritative until the configured adaptive idle timeout elapses, regardless of refresh cadence.

## Commit 10 - YAML Line-Length Lint Cleanup

- Wrapped long Jinja threshold comparisons in `config/packages/common/home_infra.yaml` template triggers to stay under the line-length limit.
- Shortened low-disk alert message label text from `Threshold` to `Warn` in two notification templates to remove remaining line-length lint errors.

Why:
- `yamllint` was failing on current line-length errors in tracked YAML, which reduced confidence in `scripts/check.sh` results and masked new lint regressions.

How to verify:
1. Activate the repo venv (`source .venv/bin/activate`) if needed.
2. Run `yamllint config/packages/common/home_infra.yaml`.
3. Confirm only the existing `document-start` warning remains and no `line-length` errors are reported.
4. (Optional) Trigger the low-disk alert automations and confirm the message still renders correctly with the `Warn:` label.

Version assumptions:
- No Home Assistant feature behavior changes assumed; this commit only adjusts YAML formatting and notification message text labels.

## Commit 11 - Home Infra Duplicate-Key Warning Cleanup

- Removed the `<<:` YAML merge pattern from the two queue-stuck automations in `config/packages/common/home_infra.yaml`.
- Expanded the radar queue-stuck automation explicitly (matching the existing sonar automation structure) and kept the shared notify script call unchanged.
- Verified Home Assistant startup logs after restart no longer show the previous duplicate-key warnings from this package.

Why:
- Home Assistant logs warned about duplicate keys while loading `home_infra.yaml` because the merge-key pattern overrode `id`, `alias`, `trigger`, and `action` keys in the same mapping.

How to verify:
1. Run `yamllint config/packages/common/home_infra.yaml` and confirm no syntax/line-length errors are reported (only the existing `document-start` warning may remain).
2. Sync `config/` to the HA host and run `ha core check`.
3. Restart Home Assistant Core and inspect startup logs.
4. Confirm duplicate-key warnings referencing `/config/packages/common/home_infra.yaml` are no longer present.

Version assumptions:
- Checked behavior against Home Assistant Core `2026.2.3` on the target host on 2026-02-24; explicit automation mappings are the safest cross-version YAML form for this package.

## Commit 12 - Disk Free SSH Known-Hosts Persistence

- Updated `config/scripts/disk_free.sh` to use `-o UserKnownHostsFile=/config/ssh/known_hosts` and `-o GlobalKnownHostsFile=/dev/null` for the SSH call to the media host.
- Kept existing key-based auth (`/config/ssh/id_ed25519`) and `BatchMode` behavior unchanged.
- Intended operational follow-up: refresh the media host key entry in `/config/ssh/known_hosts` after the IP/host move.

Why:
- `disk_free.sh` was failing with `Host key verification failed` after the media host IP change, and relying on default/root/global SSH known-hosts state is fragile in HA/container environments.

How to verify:
1. Sync `config/` to the HA host.
2. Ensure `/config/ssh/known_hosts` contains the current host key for the media host (`192.168.1.154`).
3. Run `bash /config/scripts/disk_free.sh /srv/media` on the HA host and confirm it prints a numeric GB value.
4. Check HA logs and confirm the command-line sensor host-key verification errors are gone.

Version assumptions:
- Assumes the HA Core container continues to execute command-line integrations with access to `/config` and an OpenSSH client supporting `UserKnownHostsFile` (verified pattern on current target host family).

## Commit 13 - TCP Port Probe Timeout Noise Reduction

- Updated the `binary_sensor` TCP port checks in `config/packages/common/home_infra.yaml` to send a minimal HTTP `HEAD` request instead of an empty payload.
- Added `value_template` + `value_on: '1'` for each TCP sensor so Home Assistant marks the sensor on only when the response starts with `HTTP/`.
- Verified the new config with `ha core check`, restarted HA Core, and confirmed no new `tcp.entity` timeout warnings appeared after startup in the latest log window.

Why:
- The previous config sent an empty payload (`''`) to HTTP services, which caused repeated `tcp.entity` warnings: “Timeout ... after sending ''” even though the services were running and reachable.

How to verify:
1. Run `yamllint config/packages/common/home_infra.yaml` (only the existing `document-start` warning should remain).
2. Sync `config/` to the HA host and run `ha core check`.
3. Restart Home Assistant Core.
4. Inspect recent logs and confirm the old `homeassistant.components.tcp.entity` warnings containing `after sending ''` no longer recur.

Version assumptions:
- Checked the official Home Assistant TCP integration docs and target-host behavior on 2026-02-24 (`HA Core 2026.2.3`), which require `payload` for `binary_sensor.tcp`; this fix preserves that requirement while making the probe match HTTP services.

## Commit 14 - Sidebar Overflow Burger Menu (V2)

- Updated `config/www/nx-displaygrid/components/fb-sidebar.js` to remove sidebar scrolling and dynamically measure available nav space.
- Added a bottom sidebar burger menu that appears when primary navigation items do not fit, and lists the hidden items (with labels/icons/counts).
- Kept the active screen visible in the sidebar when possible and highlighted the burger button when the active screen is in the overflow list.
- Bumped `config/lovelace/resources.yaml` `v=` query string so browsers fetch the updated sidebar component code.

Why:
- On smaller desktop heights, the collapsed V2 sidebar could scroll and hide navigation items; the requested UX is a fixed sidebar with a bottom “more” menu for overflowed items.

How to verify:
1. Sync `config/` to the HA host and run `ha core check`.
2. Hard refresh `nx-displaygrid v2` (the resource cache-bust was bumped).
3. Reduce viewport height (or use a layout with many nav items) and confirm the sidebar itself does not scroll.
4. Confirm a burger menu appears at the bottom and opens a list of the hidden menu items.
5. Click a hidden item from the burger menu and confirm navigation works.

Version assumptions:
- No HA backend behavior change is required; this is a frontend/Lit custom-card UI change compatible with the current Lovelace resource loading flow on the target host series.

## Commit 15 - Sidebar Overflow Menu Outside-Click Close

- Updated `config/www/nx-displaygrid/components/fb-sidebar.js` to register a document-level capture `pointerdown` handler while connected.
- Added outside-click detection that closes the overflow burger menu when clicking anywhere outside the sidebar host.
- Bumped `config/lovelace/resources.yaml` `v=` query string so clients load the updated sidebar behavior.

Why:
- After adding the sidebar overflow burger menu, it should close naturally when users click elsewhere in the UI.

How to verify:
1. Sync `config/` to the HA host and run `ha core check`.
2. Hard refresh `nx-displaygrid v2`.
3. Open the sidebar burger menu.
4. Click outside the sidebar (for example in the main content area).
5. Confirm the burger menu closes.

Version assumptions:
- Frontend-only Lit custom-card behavior change; relies on standard composed pointer events in the current Home Assistant/Lovelace browser runtime.

## Commit 16 - Sidebar Overflow Menu Contrast Fix

- Updated `config/www/nx-displaygrid/components/fb-sidebar.js` overflow-menu item styles to explicitly use sidebar text color for button content.
- Added explicit `ha-icon` color inheritance for overflow-menu icons to prevent white-on-white rendering.
- Bumped `config/lovelace/resources.yaml` `v=` query string so browsers load the updated sidebar styling.

Why:
- Overflow burger-menu icons (and potentially labels) could render with insufficient contrast due to button default color inheritance.

How to verify:
1. Sync `config/` to the HA host and run `ha core check`.
2. Hard refresh `nx-displaygrid v2`.
3. Open the sidebar burger menu.
4. Confirm icons and labels are clearly visible against the menu background.

Version assumptions:
- Frontend-only styling change; no HA backend behavior changes required.

## Commit 17 - Sidebar Overflow Menu On-Screen Positioning

- Updated `config/www/nx-displaygrid/components/fb-sidebar.js` collapsed-rail overflow-menu positioning to anchor the popup to the right of the sidebar instead of the sidebar's inner right edge.
- Bumped `config/lovelace/resources.yaml` `v=` query string so browsers fetch the corrected popup layout CSS.

Why:
- In the collapsed sidebar layout, the overflow burger popup could open mostly off-screen to the left, making the menu look blank even though items were rendered.

How to verify:
1. Sync `config/` to the HA host and run `ha core check`.
2. Hard refresh `nx-displaygrid v2`.
3. Open the sidebar overflow burger menu.
4. Confirm the popup appears fully on-screen to the right of the sidebar and the hidden menu items are visible/clickable.

Version assumptions:
- Frontend-only CSS positioning change for the custom Lovelace card; no HA backend behavior changes required on the current target host series.
