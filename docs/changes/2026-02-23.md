# 2026-02-23

## Commit 1 - Deployment/Docs Hardening

- Added `deploy/backup-remote-config.sh` for full remote `/config` snapshots (including `.storage`, excluding DB/logs, excluding secrets by default).
- Added `deploy/export-nx-displaygrid-setup.sh` to export the runtime `nx-displaygrid` setup (`.storage/nx_displaygrid.config` + relevant dashboard/integration files).
- Added optional pre-deploy backup behavior (enabled by default, configurable with `BACKUP_BEFORE_DEPLOY`, `HA_BACKUP_ROOT`, `HA_BACKUP_INCLUDE_SECRETS`).
- Added `scripts/check.sh` to run the repo's fast test/check workflow and skip unavailable tools gracefully.
- Removed stale legacy CI/tooling configs (`.travis.yml`, PHP/SQL lint configs) and simplified `.eslintrc.json` to match the actual JS-only repo.
- Fixed documentation drift across `README.md`, `docs/CONTRIBUTING.md`, `docs/RELEASE_CHECKLIST.md`, `ARCHITECTURE.md`, and `AGENTS.md`.
- Removed the stale resource-version comment from `config/lovelace/nx-displaygrid.yaml`.
- Updated `config/secrets.yaml.template` to include currently used secret keys.

Why:
- Deployment and data-structure changes needed a repeatable backup/export workflow so runtime setup can be restored without manual re-onboarding.
- Stale CI/tooling/docs were increasing onboarding time and reducing trust in the runbook.

How to verify:
1. Run `scripts/check.sh` and confirm Node tests pass (tool-dependent checks may be skipped if not installed).
2. Run `bash -n deploy/*.sh scripts/check.sh` and confirm no shell syntax errors.
3. Run `deploy/backup-remote-config.sh <host> lab` against a reachable HA host and confirm a timestamped snapshot is created under `backups/ha/`.
4. Run `deploy/export-nx-displaygrid-setup.sh <host> lab` and confirm the export includes `config/.storage/nx_displaygrid.config` when present.
5. Confirm `docs/RELEASE_CHECKLIST.md` references `config/lovelace/resources.yaml` for cache-busting.

Version assumptions:
- Reviewed Home Assistant official frontend custom-card docs and Lovelace resource docs plus the 2026.2 release notes/changelog on 2026-02-23.
- Assumes the custom integration store file remains `.storage/nx_displaygrid.config` (matching `Store(..., "nx_displaygrid.config")` in this repo).

## Commit 2 - Storage/Protocol Deduplication

- Added shared browser scoped-storage helpers in `config/www/nx-displaygrid/util/scoped-storage.util.js` for key naming, local JSON IO, prefix cleanup, and legacy migration.
- Refactored `prefs`, `persistence`, and `refresh` modules to use the shared storage helpers (no behavioral change intended).
- Added websocket command constants in `config/www/nx-displaygrid/util/ws-protocol.util.js` and used them in frontend persistence calls.
- Added Python websocket command constants in `config/custom_components/nx_displaygrid/websocket.py` and reduced duplicated setup code in `config/custom_components/nx_displaygrid/__init__.py`.
- Started tracking the `config/custom_components/nx_displaygrid/` backend integration files in git (they were hidden locally by a global ignore), excluding `__pycache__`.
- Added tests for scoped storage helpers and websocket command stability.
- Kept existing local frontend hardening edits intact while refactoring around them.

Why:
- Storage key/migration logic and websocket command names were duplicated across modules, increasing drift risk during schema changes and recovery workflows.
- The backend custom component must be versioned for reproducible deploys and rollback-safe setup migrations.

How to verify:
1. Run `node --test tests/*.test.mjs` and confirm all tests pass, including `scoped-storage` and `ws-protocol-commands`.
2. Run `python3 -m py_compile config/custom_components/nx_displaygrid/__init__.py config/custom_components/nx_displaygrid/websocket.py`.
3. In HA, load the dashboard and confirm persisted `nx-displaygrid` config still loads and saves (WS when backend is available, local fallback otherwise).

## Commit 3 - View/Sorting Deduplication

- Added `config/www/nx-displaygrid/views/repeat.util.js` to centralize the Lit `repeat` fallback used when the directive is unavailable.
- Refactored `mobile`, `month`, `schedule`, `shopping`, `important`, and `chores` views to use the shared fallback helper (no render behavior change intended).
- Extracted a shared `compareTimedEventPosition(...)` comparator in `nx-displaygrid.util.js` and reused it in `schedule.layout.js`.
- Added `tests/event-sort.test.mjs` to lock timed-event sort ordering behavior.

Why:
- These duplicate view helpers and sort comparators were small but repeated in multiple files, increasing drift risk and making future layout changes harder to verify.

How to verify:
1. Run `node --test tests/*.test.mjs` and confirm all tests pass, including `event-sort`.
2. Load `nx-displaygrid` in Home Assistant and confirm `mobile`, `month`, `schedule`, `shopping`, `important`, and `chores` views render normally.
3. In `Schedule`, verify overlapping timed events still order consistently by start time, end time, then stable key.

Version assumptions:
- No Home Assistant API behavior changes assumed; this is an internal JS refactor only.

## Commit 4 - Home Controls Controllability Guard

- Added `isControllableEntity(hass, entityId)` in `nx-displaygrid.util.js` to centralize Home Controls eligibility checks.
- Updated the Home Controls add handler to block non-controllable/hidden entities and show a toast instead of saving invalid entries.
- Updated config validation to flag unavailable/hidden Home Controls using the same controllability rule.

Why:
- Home Controls were only checking entity existence, which allowed sensors or non-toggleable entities to be saved and later fail in UI interactions.

How to verify:
1. Run `node --test tests/*.test.mjs` and confirm the test suite still passes.
2. In Home Assistant, open `nx-displaygrid` settings and try adding a sensor/binary_sensor as a Home Control; confirm the card shows `Not a controllable entity`.
3. Add a valid controllable entity (`light.*`, `switch.*`, `input_boolean.*`, or `fan.*`) and confirm it is still accepted.
4. Load a config containing a hidden/removed Home Control entity and confirm validation reports `Home control unavailable (hidden): <entity_id>`.

Version assumptions:
- Assumes Home Controls are intended for domains with explicit `toggle` or paired `turn_on`/`turn_off` services in Home Assistant.

## Commit 5 - Default Config Source of Truth

- Extended `nx-displaygrid.defaults.js` with `DEFAULT_CARD_CONFIG` for runtime card defaults (`days_to_show`, day hours, slot sizing, refresh interval).
- Updated `nx-displaygrid-card.js` stub config to reuse the shared defaults.
- Updated setup/runtime fallback logic, settings fallback hour values, and YAML export helpers (`nx-displaygrid.config.js`, `fb-manage-sources.js`) to read from the shared defaults.

Why:
- Default values were duplicated across multiple runtime and editor paths, and some values had already drifted, making future behavior changes risky and inconsistent.

How to verify:
1. Run `node --test tests/*.test.mjs` and confirm all tests pass.
2. In Lovelace, add a new `nx-displaygrid` card and confirm the stub/default values match current behavior (5 days, 06:00 start, 24:00 end, 30-minute slots).
3. Open Settings and verify day start/end defaults still show `6`/`24` when not explicitly configured.
4. Export/copy YAML from source/config helpers and confirm `days_to_show` remains `5`.

Version assumptions:
- No Home Assistant API changes assumed; this only centralizes internal frontend defaults already in use.

## Commit 6 - Refresh Retry Helper Deduplication

- Refactored `nx-displaygrid.refresh.js` to use shared file-local helpers for retry scheduling and clearing (`_scheduleRetryTimer`, `_clearRetryTimer`).
- Updated calendar, todo, and shopping retry paths to delegate to the shared helpers while preserving their existing caps and retry flags.

Why:
- Retry timer logic was repeated in three refresh paths, which made small behavior changes (retry cap, timer cleanup, flags) easy to drift over time.

How to verify:
1. Run `node --test tests/*.test.mjs` and confirm all tests pass.
2. In Home Assistant, temporarily disable a calendar/todo/shopping backend and confirm retries still occur and the UI recovers after the backend is restored.
3. Confirm manual refresh still works and does not leave retry state stuck after successful data refresh.

Version assumptions:
- No Home Assistant API changes assumed; this is an internal frontend refactor of retry timer orchestration only.

## Commit 7 - Settings Render-State Helper Extraction

- Moved static weekday/bin-colour option arrays in `settings.view.js` to file-level constants.
- Extracted file-local helper functions for rotation pattern labels, people-display ordering, refresh age/timestamp formatting, and refresh reason labels.
- Simplified the `render()` method to consume those helpers without changing settings UI behavior.

Why:
- `settings.view.js` is the largest frontend maintenance hotspot; pulling pure derivation logic out of `render()` reduces cognitive load and makes future splitting into sections safer.

How to verify:
1. Run `node --test tests/*.test.mjs` and confirm all tests pass.
2. Run `node --check config/www/nx-displaygrid/views/settings.view.js`.
3. Open `nx-displaygrid` Settings in Home Assistant and confirm people display ordering, bin rotation pattern preview, and refresh status labels still render as before.

Version assumptions:
- No Home Assistant API changes assumed; this is an internal frontend render-logic refactor only.

## Commit 8 - Shared Config YAML Serializer

- Added `config/www/nx-displaygrid/util/config-yaml.util.js` with `serializeNxDisplaygridCardConfig(...)` for reusable YAML serialization.
- Refactored `nx-displaygrid.config.js` and `components/fb-manage-sources.js` to use the shared serializer (with flags for optional `background_theme` and `home_controls` sections).
- Added `tests/config-yaml-serializer.test.mjs` to lock serializer behavior in both modes.

Why:
- YAML serialization for `nx-displaygrid` config export/copy was duplicated in two places and already diverging by optional sections, increasing drift risk during config-schema changes.

How to verify:
1. Run `node --test tests/*.test.mjs` and confirm all tests pass (including `config-yaml-serializer`).
2. In HA, open `Manage sources`, use Copy config, and confirm the copied YAML still matches the previous shape (no `background_theme`/`home_controls` sections added there).
3. Export YAML via the card/editor flow and confirm optional `background_theme` and `home_controls` are still included when configured.

Version assumptions:
- No Home Assistant API changes assumed; this refactor only consolidates internal YAML string generation for existing config fields.

## Commit 9 - Refresh Retry Characterization Tests

- Added `tests/refresh-retry.test.mjs` to characterize `nx-displaygrid.refresh.js` retry scheduling/clearing behavior for calendar/todo/shopping refresh paths.
- Covered retry base delays, retry caps, retrying flags, callback queue arguments, and `_queueRefresh(...)` pending state behavior when a refresh is already running.

Why:
- The refresh pipeline is a long-term complexity hotspot; characterization tests make future simplification safer by locking current retry semantics before structural refactors.

How to verify:
1. Run `node --test tests/*.test.mjs` and confirm all tests pass (including `refresh-retry`).
2. Review `tests/refresh-retry.test.mjs` and confirm expected retry caps match current behavior:
   - calendar retries cap at `refresh_interval_ms`
   - todo/shopping retries cap at `max(refresh_interval_ms, 120000)`

Version assumptions:
- No Home Assistant API changes assumed; this commit adds tests only.

## Commit 10 - Settings Render Section Split (Phase 2)

- Extracted the admin-locked settings branch in `views/settings.view.js` to `_renderAdminLocked(...)`.
- Extracted reset/info dialog footer rendering in `views/settings.view.js` to `_renderDialogs(...)`.
- Kept the main settings panel markup and behaviors unchanged while reducing `render()` size and nesting.

Why:
- `settings.view.js` remains the largest maintenance hotspot; isolating major template branches makes future section extraction safer and easier to review.

How to verify:
1. Run `node --check config/www/nx-displaygrid/views/settings.view.js`.
2. Run `node --test tests/*.test.mjs` and confirm all tests pass.
3. In HA, confirm:
   - locked admin flow still prompts for PIN,
   - reset preferences/dashboard confirmation dialogs still behave the same,
   - info dialogs still open/close as before.

Version assumptions:
- No Home Assistant API changes assumed; this is an internal render-structure refactor only.

## Commit 11 - Shared Source Validation Rules (Phase 1)

- Added `config/www/nx-displaygrid/util/source-validation.util.js` with shared helpers:
  - `configHasPeople(...)`
  - `configHasSources(...)`
  - `configHasSourceData(...)`
- Updated `nx-displaygrid.validation.js` startup no-sources check to use `configHasSources(...)`.
- Updated `fb-manage-sources.js` config-data detection to use `configHasSourceData(...)` while preserving the existing fallback for non-source config keys.
- Added `tests/source-validation-util.test.mjs`.

Why:
- Basic source/business-rule checks were duplicated and beginning to diverge across runtime and editor flows. A shared helper reduces drift and gives future setup/editor refactors a stable base.

How to verify:
1. Run `node --test tests/*.test.mjs` and confirm all tests pass (including `source-validation-util`).
2. Start the dashboard without configured sources and confirm the existing “No sources configured” startup toast behavior is unchanged.
3. Open Manage Sources and confirm existing open/save behavior is unchanged.

Version assumptions:
- No Home Assistant API changes assumed; this commit centralizes internal source-presence logic only.

## Commit 12 - Restore Runbook for Backup/Export Workflow

- Added `docs/RESTORE.md` with step-by-step recovery paths for:
  - full `/config` rollback (including `.storage`)
  - focused `nx-displaygrid` restore (stored config/dashboard/resources/custom integration files)
- Linked the restore runbook from `README.md` and `docs/RELEASE_CHECKLIST.md`.

Why:
- Backup/export scripts were in place, but recovery under pressure was still tribal knowledge. A documented restore path makes schema/data-structure changes safer and faster to recover from.

How to verify:
1. Read `docs/RESTORE.md` and confirm paths match your environment (`lab`/`live`, SSH host, backup folder structure).
2. Confirm `README.md` and `docs/RELEASE_CHECKLIST.md` point to `docs/RESTORE.md`.
3. (Optional dry run) list a backup snapshot folder and verify the documented `rsync` source paths exist.

Version assumptions:
- Assumes Home Assistant targets are reachable via SSH as `root` and support `ha core restart` (or equivalent restart command documented in repo deploy scripts).

## Commit 13 - Settings Cache-Status Render Split + Home Control Eligibility Tests

- Extracted the Preferences debug/cache-status subsection in `views/settings.view.js` into `_renderPreferencesDebugCacheStatus(...)`.
- Reused `configHasPeople(...)` in `nx-displaygrid.config.js` for onboarding-required checks (shared source rule utility adoption).
- Added `tests/controllable-entity.test.mjs` to lock `isControllableEntity(...)` behavior used by Settings/Home Controls add+validation flows.

Why:
- `settings.view.js` still has large template branches; extracting high-churn subsections lowers review risk.
- Home Controls eligibility is a settings-critical rule and now has direct unit coverage.

How to verify:
1. Run `node --check config/www/nx-displaygrid/views/settings.view.js`.
2. Run `node --test tests/*.test.mjs` and confirm all tests pass (including `controllable-entity`).
3. In HA, open Settings with debug enabled and confirm cache status/tools still render and function normally.
4. Confirm first-run/onboarding behavior is unchanged for a config with and without people configured.

Version assumptions:
- No Home Assistant API changes assumed; this commit refactors render structure and adds utility tests only.

## Commit 14 - Add V2 Dashboard Copy + Feature Flag Scaffold

- Added a new Lovelace YAML dashboard file at `config/lovelace/nx-displaygrid-v2.yaml` with the same baseline card configuration as `nx-displaygrid`.
- Registered `nx-displaygrid-v2` in `config/configuration.yaml` so it appears as a separate sidebar dashboard and leaves the original dashboard untouched.
- Added card helpers `_v2Features(...)` / `_v2FeatureEnabled(...)` in `config/www/nx-displaygrid/nx-displaygrid.config.js` for isolating upcoming V2-only functionality.

Why:
- The requested V2 build needs a safe rollout path where new functionality can be enabled on a cloned dashboard without changing the existing `nx-displaygrid` experience.

How to verify:
1. Run `node --check config/www/nx-displaygrid/nx-displaygrid.config.js`.
2. In Home Assistant, confirm a new sidebar dashboard `nx-displaygrid v2` appears and loads the same baseline card content as `nx-displaygrid`.
3. Confirm the original `nx-displaygrid` dashboard remains present and unchanged.

## Commit 15 - V2 Family and Admin Dashboard Views

- Added V2 feature flags in `config/lovelace/nx-displaygrid-v2.yaml` for `family_dashboard` and `admin_dashboard`.
- Added dedicated `Family` and `Admin` view components:
  - `config/www/nx-displaygrid/views/family.view.js` (daily-use summary, common actions, people/upcoming)
  - `config/www/nx-displaygrid/views/admin.view.js` (system health + operations/recovery controls)
- Wired the new views into `nx-displaygrid-card.js` rendering/imports.
- Updated the Settings default landing-view selector to include `Family` and `Admin` when available (admin gated by access).

Why:
- Your V2 backlog explicitly called for separate family-focused and admin-focused dashboard surfaces; these views provide that split without altering the original dashboard or existing V2 screens.

How to verify:
1. Run `node --check config/www/nx-displaygrid/nx-displaygrid-card.js config/www/nx-displaygrid/views/family.view.js config/www/nx-displaygrid/views/admin.view.js config/www/nx-displaygrid/views/settings.view.js`.
2. Run `node --test tests/*.test.mjs` and confirm all tests pass.
3. Run `scripts/check.sh` and confirm the repo check wrapper passes (tool-dependent steps may be skipped).
4. Open `nx-displaygrid v2` in Home Assistant and confirm `Family` appears in navigation.
5. Confirm `Admin` appears only for admin/unlocked users and shows system health + recovery actions.
6. In `Settings`, confirm `Default landing view` includes `Family` (and `Admin` when admin access is available).

Version assumptions:
- Assumes existing `nx-displaygrid` V2 feature-flag navigation plumbing (`_v2NavScreens`) remains the source of truth for optional screens, and Home Assistant custom-card rendering behavior is unchanged in the current 2026.2 frontend APIs.

Version assumptions:
- Reviewed Home Assistant custom-card/Lovelace YAML dashboard/resource docs plus 2026.2 release notes/changelog on 2026-02-23; this change only adds a second YAML dashboard entry and frontend config flags, and assumes `lovelace.resource_mode: yaml` remains supported as currently configured.

## Commit 15 - V2 Important/Chores Metadata Additions

- Added a V2-only `Important` view header chip that shows the current shopping list count (quantity-aware) with a shopping icon.
- Added V2-only chore metadata badges in `views/chores.view.js` for available Todoist-style fields: priority, labels/tags, subtasks, comments, and recurring status.
- Kept both changes gated behind `v2_features` flags so the original `nx-displaygrid` dashboard render remains unchanged.

Why:
- These are explicit CONCEPT.md expectations that were still missing or only partially surfaced in the UI, and they improve “at a glance” wall-display usefulness without changing backend integrations.

How to verify:
1. Run `node --check config/www/nx-displaygrid/views/important.view.js`.
2. Run `node --check config/www/nx-displaygrid/views/chores.view.js`.
3. Run `node --test tests/*.test.mjs` and confirm no regressions.
4. Open `nx-displaygrid v2` in Home Assistant:
   - Confirm `Important` shows a shopping count chip in the header.
   - Confirm `Chores` items display metadata badges when Todo items include labels/priority/comments/subtasks/recurring fields.
5. Open the original `nx-displaygrid` dashboard and confirm those new UI elements are not shown there.

Version assumptions:
- No Home Assistant API behavior changes assumed; these UI additions only read fields already returned by configured todo/shopping entities and render them when present.

## Commit 16 - V2 Date Context Persistence + Broader Home Controls

- Added V2-only persisted date context in prefs (`lastMainMode`, `scheduleDayOffset`, `monthOffset`) so month/schedule navigation state survives reloads on the V2 dashboard.
- Added `_isHomeControlEntityEligible(...)` card helper and switched Home Controls add/validation/render/settings lists to use it consistently.
- Extended `isControllableEntity(...)` with an optional `allowAllDomains` mode (backed by HA service-registry checks, including generic `homeassistant.turn_on/turn_off`) and added unit tests covering broader-mode behavior.

Why:
- `CONCEPT.md` calls for schedule date context persistence and broader Home Controls coverage than the current strict allowlist; V2 is the right place to add both without changing current-dashboard behavior.

How to verify:
1. Run `node --test tests/controllable-entity.test.mjs`.
2. Run `node --test tests/*.test.mjs` and confirm all tests pass.
3. Open `nx-displaygrid v2`, switch between `Schedule`/`Month`, navigate dates, reload the page, and confirm the previous mode/date context returns.
4. In `nx-displaygrid v2` Settings -> Home controls, confirm entities outside the old allowlist appear when HA exposes on/off support via domain or generic `homeassistant` services.
5. Open the original `nx-displaygrid` dashboard and confirm Home Controls eligibility remains on the existing strict behavior.

Version assumptions:
- Assumes Home Assistant continues exposing service registry metadata on `hass.services` (including `homeassistant.turn_on` / `homeassistant.turn_off`) as documented in current frontend/custom-card APIs; when unavailable, V2 broader eligibility falls back conservatively instead of widening by guesswork.

## Commit 17 - Bump Lovelace Resource Version for V2 Frontend Changes

- Updated the `config/lovelace/resources.yaml` cache-bust query string for `/local/nx-displaygrid/nx-displaygrid.js`.
- Recorded the resource bump alongside the V2 dashboard/frontend changes so deploys force a browser reload of the updated JS.

Why:
- Home Assistant/Lovelace aggressively caches custom card resources; after changing `nx-displaygrid` frontend modules, the resource `v=` should be bumped to avoid stale JS on clients.

How to verify:
1. Confirm `config/lovelace/resources.yaml` now references `/local/nx-displaygrid/nx-displaygrid.js?v=20260223-203528`.
2. After deploy/restart, hard refresh the browser and confirm `nx-displaygrid v2` shows the new V2-only features.
3. Confirm the original `nx-displaygrid` dashboard still loads (with unchanged non-V2 behavior).

Version assumptions:
- Reviewed Home Assistant Lovelace resource/YAML dashboard docs and 2026.2 release notes on 2026-02-23; manual cache-bust version bumps remain the safe workflow for non-bundled custom-card JS updates in this repo.

## Commit 18 - V2 Food View (Menu + Inventory + Saved Lists/Meals)

- Added V2-only `Food` screen plumbing to sidebar/topbar navigation and main card rendering.
- Added `config/www/nx-displaygrid/views/food.view.js` with two tabs:
  - `Menu` (weekly meal planning)
  - `In the house` (pantry/fridge inventory lists)
- Added `config/www/nx-displaygrid/nx-displaygrid.food.js` helpers to persist V2 food data in shared card config (`food_v2`) and support:
  - custom saved shopping lists
  - custom meals with saved shopping items
  - one-click add-to-shopping for saved lists, meals, and menu day meals

Why:
- This covers the highest-value Phase 1 additions from the requested V2 roadmap using existing dashboard persistence and shopping-list flows, without requiring new backend integrations.

How to verify:
1. Run `node --check config/www/nx-displaygrid/nx-displaygrid.food.js`.
2. Run `node --check config/www/nx-displaygrid/views/food.view.js`.
3. Run `node --check config/www/nx-displaygrid/components/fb-topbar.js`.
4. In Home Assistant, open `nx-displaygrid v2` and confirm a new `Food` screen is available.
5. In `Food -> Menu`, create a meal and use `Add to shopping`; confirm items appear in Shopping.
6. In `Food -> In the house`, add pantry/fridge items and reload; confirm they persist.
7. Confirm the original `nx-displaygrid` dashboard does not show a `Food` screen.

Version assumptions:
- No Home Assistant API behavior changes assumed; this feature persists V2 food/menu/inventory data through existing `nx-displaygrid` shared config storage and reuses the existing shopping todo integration for add-to-shopping actions.

## Commit 19 - V2 Extended Event Details (Location/Description)

- Added V2-only extended event detail fields (`Location`, `Description`) to `fb-event-dialog`.
- Passed location/description through the calendar event update service path in `calendar.service.js` and handler wiring.
- Kept the original dashboard behavior unchanged by gating the extra fields behind the V2 `event_details_extended` feature flag.

Why:
- The requested V2 scope explicitly called for event details beyond title/time; surfacing and preserving location/description in the existing event dialog is the safest incremental implementation.

How to verify:
1. Run `node --check config/www/nx-displaygrid/components/fb-event-dialog.js`.
2. Run `node --check config/www/nx-displaygrid/services/calendar.service.js`.
3. Run `node --test tests/*.test.mjs`.
4. In `nx-displaygrid v2`, open an event dialog and confirm location/description fields appear.
5. Edit an event on a writable calendar, save, and confirm location/description changes persist.
6. Confirm the original `nx-displaygrid` event dialog still shows the pre-V2 field set.

Version assumptions:
- Assumes current Home Assistant calendar services continue accepting optional `location` and `description` fields when supported by the underlying calendar provider/integration; read-only calendars remain unchanged.

## Commit 20 - V2 Settings Person Wizard UI (Add/Edit)

- Enabled a V2-only Settings person wizard UI (using the existing person-wizard draft/apply logic already present in `settings.view.js`).
- Added `Add person (wizard)` and `Edit` entry points in the People section of Settings when the V2 feature flag is enabled.
- Added step-by-step modal flow with:
  - Basics (name, id, colour, role, header row)
  - Calendar associations
  - Todo associations
  - Confirm/review
- Added Back/Next navigation without draft data loss, and Cancel closes/discards the draft.

Why:
- This was explicitly requested for V2 and the underlying mutation logic was already available; finishing the UI path provides a safe, reviewable improvement without reworking onboarding.

How to verify:
1. Run `node --check config/www/nx-displaygrid/views/settings.view.js`.
2. Run `node --test tests/*.test.mjs`.
3. In `nx-displaygrid v2` -> `Settings` -> `People`, open `Add person (wizard)`.
4. Move back/next across steps and confirm entered values persist until cancel/confirm.
5. Confirm Cancel closes the wizard without changing people/calendars/todos.
6. Confirm Confirm updates person details and calendar/todo assignments.
7. Confirm the original `nx-displaygrid` Settings view does not show the V2 wizard buttons.

Version assumptions:
- No Home Assistant API changes assumed; this is a frontend Settings workflow using existing persisted config structures (`people`, `calendars`, `todos`, `people_display`).

## Commit 21 - V2 Intent + Ambient Views (Adaptive Actions)

- Added `config/www/nx-displaygrid/nx-displaygrid.intent.js` with V2 helpers for:
  - house mode state lookup (optional configured entity)
  - adaptive intent action selection using time-of-day, recent refresh state, chores/shopping counts, and current events
  - ambient summary data aggregation
- Added `views/intent.view.js` (mobile-first, large action buttons, minimal context cards).
- Added `views/ambient.view.js` (tablet-friendly ambient dashboard with glanceable stats and upcoming events).
- Wired V2 `Intent` and `Ambient` screens into main card rendering/navigation.

Why:
- This delivers the requested Phase 1 intent/ambient surfaces and adaptive action behavior entirely in the frontend, with optional house-mode enrichment when an entity is configured later.

How to verify:
1. Run `node --check config/www/nx-displaygrid/nx-displaygrid.intent.js`.
2. Run `node --check config/www/nx-displaygrid/views/intent.view.js`.
3. Run `node --check config/www/nx-displaygrid/views/ambient.view.js`.
4. Run `node --test tests/*.test.mjs`.
5. In `nx-displaygrid v2`, confirm `Intent` and `Ambient` screens appear in navigation.
6. In `Intent`, confirm large action buttons appear and differ by time of day / stale-refresh conditions.
7. In `Ambient`, confirm glanceable stats and upcoming events render and open event dialogs when tapped.
8. Confirm the original `nx-displaygrid` dashboard does not show these screens.

Version assumptions:
- Assumes optional house-mode enrichment can read a Home Assistant entity from `config.intent_v2.house_mode_entity` if present; otherwise intent/ambient behavior uses local dashboard state/time-only fallbacks.

## Commit 22 - V2 Adaptive Presentation + House Mode Scaffolding (Phase 2)

- Added `config/www/nx-displaygrid/nx-displaygrid.adaptive.js` with V2 helpers for:
  - allowed V2 view persistence (`food`/`intent`/`ambient`)
  - explicit house mode scaffolding defaults (`home`, `away`, `away_off`, `guest`, `quiet`, `calm`)
  - occupancy/entity lookups
  - dynamic theme palette selection (day/night, alerts/stale states, occupancy, house mode)
  - automatic recommended screen selection/switching (time, occupancy, events, board health)
- Wired adaptive presentation ticks into card updates and refresh completion, and applied dynamic theme overlays after config setup.
- Added V2 Settings controls for house mode entity, occupancy entity, auto-screen switching, idle delay, and dynamic theme enable/disable.
- Added house-mode quick action chips to the V2 `Intent` view (for `input_select` / `select` entities when configured).
- Updated `nx-displaygrid-v2.yaml` with Phase 2 V2 feature flags and scaffold config blocks (`adaptive_v2`, `intent_v2`, `house_modes_v2`).
- Bumped Lovelace resource cache-bust query string to force clients to load the new frontend code.

Why:
- This implements the core Phase 2 goals (explicit mode scaffolding, dynamic themes, and auto-adaptive layout/content behavior) using safe frontend logic and optional HA entities, without requiring backend schema migrations or new integrations.

How to verify:
1. Run `node --check config/www/nx-displaygrid/nx-displaygrid.adaptive.js`.
2. Run `node --check config/www/nx-displaygrid/views/settings.view.js`.
3. Run `node --check config/www/nx-displaygrid/views/intent.view.js`.
4. Run `node --test tests/*.test.mjs`.
5. In `nx-displaygrid v2` Settings, configure:
   - `House mode entity` (optional)
   - `Occupancy entity` (optional)
   - `Auto screen switching`
   - `Dynamic themes`
6. Leave the board idle and confirm V2 can switch between `Intent` / `Ambient` / `Important` / `Schedule` based on time/state.
7. Trigger stale/error conditions (or wait for retries) and confirm theme/action priority shifts toward warning/critical presentation.
8. Confirm original `nx-displaygrid` remains unchanged.
9. Hard refresh after deploy so the new `v=20260223-211347` resource loads.

Version assumptions:
- Assumes Home Assistant continues exposing entity state via `hass.states` and service registry metadata for `select` / `input_select` entities used for optional house-mode quick actions.
- Dynamic theme overlays intentionally operate only at CSS-token level and do not require Home Assistant theme changes or frontend reloads.

## Commit 23 - V2 Timed Reminder Banners + Sound Test (Phase 3)

- Added `config/www/nx-displaygrid/nx-displaygrid.reminders.js` with V2 reminder helpers for:
  - timed reminder matching (daily or selected weekdays)
  - countdown windows
  - dismiss-per-occurrence behavior
  - optional sound playback (Web Audio API beep patterns)
  - banner visibility suppression hook (when reminder content is already visible on the current dashboard)
- Added V2 reminder banner rendering in the main card with countdown/due/late messaging and dismiss/test-sound actions.
- Added V2 Settings reminder management UI:
  - add/remove reminders
  - label/message/time
  - countdown minutes
  - enabled/sound toggles
  - sound pattern selection + test button
  - weekday selection
- Added V2 reminder/notification config scaffold fields in `config/lovelace/nx-displaygrid-v2.yaml` (`notifications_v2.suppress_when_visible`).

Why:
- This implements the first Phase 3 request directly in the dashboard (timed reminder banners + optional countdown/sound + Settings test sound) while reusing existing V2 config persistence and avoiding backend dependencies.

How to verify:
1. Run `node --check config/www/nx-displaygrid/nx-displaygrid.reminders.js`.
2. Run `node --check config/www/nx-displaygrid/views/settings.view.js`.
3. Run `node --test tests/*.test.mjs`.
4. In `nx-displaygrid v2` Settings, add a reminder a few minutes ahead and use `Test reminder sound`.
5. Wait for the reminder window and confirm a banner appears with countdown (then due state), and optional sound plays once.
6. Dismiss the banner and confirm it stays dismissed for that occurrence.
7. Confirm original `nx-displaygrid` does not show reminder UI or banners.

Version assumptions:
- Assumes a browser/WebView with Web Audio API support for reminder sounds; when unavailable, the dashboard falls back to non-audio behavior (banner + toast feedback).

## Commit 24 - V2 Notification Policy Scaffolding (Severity + Context + Suppression)

- Extended `nx-displaygrid.notifications.js` with V2 notification policy helpers:
  - policy parsing (`enabled`, `notify_service`, `min_severity`, `suppress_when_visible`)
  - severity ranking/mapping for dashboard errors
  - visible-screen mapping for suppression
  - HA `notify.*` service call with clear reason + supporting context (screen, category, refresh/state summary)
- Hooked `_reportError(...)` into the V2 notification pipeline (still toast-first locally).
- Added V2 Settings controls for notification policy configuration and suppression behavior.

Why:
- This delivers the requested Phase 3 notification behavior foundation (severity, clear reasons/context, suppress when already visible on the dashboard) without forcing a specific mobile app or notification integration.

How to verify:
1. Run `node --check config/www/nx-displaygrid/nx-displaygrid.notifications.js`.
2. Run `node --check config/www/nx-displaygrid/views/settings.view.js`.
3. Run `node --test tests/*.test.mjs`.
4. In `nx-displaygrid v2` Settings, enable phone notifications and set `notify_service` to your HA mobile app notify service.
5. Trigger an error (for example, temporarily break a source/integration), and confirm:
   - a local toast still appears,
   - HA notify service receives a message when severity threshold allows,
   - messages include reason/context lines,
   - notifications are suppressed when the issue is already visible on the matching dashboard screen (if suppression is enabled).

Version assumptions:
- Assumes Home Assistant `notify.*` services accept `title`, `message`, and optional `data` payloads in the standard `callService` path used by custom cards.
