# 2026-02-26

## Food favourites + chip layout polish

### What changed
- Food view navigation now includes a dedicated `Favourites` tab.
- Added favourites panel toggle at the top of the panel:
  - `Favourite meals` (recipes with average rating `>= 3`).
  - `Favourite shopping` (items starred in shopping).
- Improved Food weekly meal-plan row responsiveness:
  - Per-day actions (`Add to shopping`, `Begin cooking`) are grouped in a responsive action block.
  - Layout now reflows cleanly on narrower widths to avoid squashed controls.
- Split shopping concerns in Food:
  - `Shopping List` remains separate.
  - Saved shopping-list bundles are rendered in shopping context instead of crowding the meal-plan panel.
- Updated topbar person chips (used in schedule/important/chores contexts):
  - Fixed 4 equal-width chips per row.
  - Counts moved below the name in a stacked layout.
  - Long names now clip with ellipsis to avoid chip distortion.
  - On mobile widths, chips switch to circular avatar mode with bold first-letter initials while preserving active-toggle highlight behavior.
  - Duplicate initials are now disambiguated on mobile (uses two-letter badges when needed).
- Shopping star consistency fix:
  - Only explicitly starred shopping items are shown with active star state.
  - Common/history quick-add items no longer show as starred unless actually starred.
- Updated Lovelace resource cache-bust:
  - `/local/nx-displaygrid/nx-displaygrid.js?v=20260226-141200`

### Why it changed
- To stabilise layout on smaller screens and implement the requested favourites split/toggle behavior while keeping chip rows visually consistent with long names.

### How to verify
1. Run checks:
   - `bash scripts/check.sh`
   - `bash scripts/check_structure.sh`
2. Hard refresh the browser after deploy (cache-bust updated).
3. In Food view:
   - Confirm tabs include `Meals`, `Recipes`, `Shopping List`, `Favourites` (and `Cooking` only when active).
   - In `Favourites`, switch between `Favourite meals` and `Favourite shopping` using the top toggle.
   - Confirm favourite meals list only includes recipes with average rating >= 3.
   - Confirm favourite shopping list shows starred shopping entries and can add/unstar.
4. In Meals view, reduce viewport width and confirm day rows/actions no longer collapse into unusable narrow buttons.
5. In schedule/chores/important top chips:
   - Confirm 4 chips per row with equal width.
   - Confirm counts are displayed beneath each name.
   - Confirm long names are ellipsized and no chip overlap/squash occurs.
   - On a narrow/mobile viewport, confirm chips become circles with initial letters and active toggles remain visually highlighted.
   - If two names share the same first letter, confirm badges are disambiguated (e.g. `AL`, `AN`).
6. In Shopping view:
   - Confirm only truly starred items show filled/active stars.
   - Confirm common/history items without starring show outline stars.

## Food shopping tab favourites cleanup

### What changed
- Added a `hideFavourites` option to `fb-shopping-view`.
- Food `Shopping List` now embeds shopping view with `hideFavourites=true`, so only the shopping list panel is shown there.
- Standalone `Shopping` screen remains unchanged and still shows the favourites panel.
- Updated Lovelace resource cache-bust:
  - `/local/nx-displaygrid/nx-displaygrid.js?v=20260226-105122`

### Why it changed
- To remove duplicated favourites UI from the Food `Shopping List` tab now that favourites are managed in the dedicated Food `Favourites` tab.

### How to verify
1. Run checks:
   - `bash scripts/check.sh`
   - `bash scripts/check_structure.sh`
2. Hard refresh the browser after deploy (cache-bust updated).
3. In Food -> `Shopping List`:
   - Confirm only `Saved Shopping Lists` and the shopping list panel are visible.
   - Confirm there is no side `Favourites` panel in this tab.
4. In standalone `Shopping` screen:
   - Confirm the right-side `Favourites` panel is still present and functional.

## Topbar chip cleanup

### What changed
- Removed event/chore count metrics from topbar people chips in schedule/important/chores.
- Updated chip layout to left-align names on desktop/tablet.
- Updated hover title text to show only the person name (no count summary).
- Updated Lovelace resource cache-bust:
  - `/local/nx-displaygrid/nx-displaygrid.js?v=20260226-105621`

### Why it changed
- To simplify the chip design and match the requested cleaner look.

### How to verify
1. Run checks:
   - `bash scripts/check.sh`
   - `bash scripts/check_structure.sh`
2. Hard refresh the browser after deploy (cache-bust updated).
3. In `Schedule`, `Chores`, and `Important` views:
   - Confirm person chips show only color dot/icon + name (no numeric counts).
   - Confirm person names are left-aligned in each chip.

## Food favourites shopping list parity

### What changed
- Updated Food `Favourites` -> `Favourite shopping` to render the same deduplicated combined list used by Shopping tab favourites (`shoppingFavourites + shoppingCommon`).
- Added per-row star action label switching (`Star`/`Unstar`) based on current starred state.
- Updated section hint text to reflect starred plus common quick-add entries.
- Updated Lovelace resource cache-bust:
  - `/local/nx-displaygrid/nx-displaygrid.js?v=20260226-113519`

### Why it changed
- To keep the moved Food favourites shopping section consistent with the list users previously saw in the Shopping tab favourites panel.

### How to verify
1. Run checks:
   - `bash scripts/check.sh`
   - `bash scripts/check_structure.sh`
2. Hard refresh the browser after deploy (cache-bust updated).
3. In Food -> `Favourites` -> `Favourite shopping`:
   - Confirm previously visible shopping quick-add entries are listed.
   - Confirm each row can be added to shopping and can be starred/unstarred.

## Food favourites shopping styling restore

### What changed
- Restyled Food `Favourites` -> `Favourite shopping` rows to match the prior shopping-favourites visual pattern:
  - bordered pill row,
  - inline green `+` quick-add marker,
  - right-side star icon with filled/outline state.
- Row click/keyboard Enter+Space adds the item to shopping; star icon toggles favourite state without triggering add.
- Updated Lovelace resource cache-bust:
  - `/local/nx-displaygrid/nx-displaygrid.js?v=20260226-120816`

### Why it changed
- To bring back the previous, denser favourites-row styling that was easier to scan than the interim text-button layout.

### How to verify
1. Run checks:
   - `bash scripts/check.sh`
   - `bash scripts/check_structure.sh`
2. Hard refresh the browser after deploy (cache-bust updated).
3. In Food -> `Favourites` -> `Favourite shopping`:
   - Confirm each item renders as a compact pill row with inline `+` and star icon.
   - Confirm clicking row body adds the item to shopping.
   - Confirm clicking star toggles between filled and outline without adding the item.

## One-terminal sync/restart/log helper

### What changed
- Added executable helper script:
  - `deploy/rsync-restart-logcheck.sh`
- The script stages env config (`lab`/`live`), runs `rsync`, restarts HA Core, waits (default 300s), then fetches and scans log output for warning/error patterns.
- Added usage docs in `README.md` under deploy workflow.

### Why it changed
- To remove the need for two terminals when doing routine deploy + restart + post-restart log checks.

### How to verify
1. Run:
   - `deploy/rsync-restart-logcheck.sh --help`
2. Dry-run usage pattern with your host:
   - `deploy/rsync-restart-logcheck.sh 100.77.2.5 live`
3. Confirm output shows:
   - rsync step,
   - restart step,
   - wait step,
   - fetched logs,
   - warning/error summary line.

## Family dashboard kiosk/chips + mobile menu cleanup

### What changed
- Updated people chips in topbar (schedule/important/chores):
  - removed kid/grownup role icons,
  - increased name prominence and left-aligned names,
  - added configurable columns via `family_dashboard_v3.people_chips_per_row` (default `5`) with capped per-chip width (`35%`),
  - kept compact/mobile circular initial-chip mode with active-state highlight.
- Set person chip filters to default to “all selected” after hard refresh/first load.
- Added family dashboard header title config (`family_dashboard_v3.title`) and wired family header to use it (default `Family Dashboard`).
- Fixed date-next/date-prev/today/date-set adaptive bounce by locking adaptive auto-screen after manual date navigation; added regression test.
- Reworked compact overflow (3-dot) behavior:
  - now shown only in compact/small-screen mode,
  - menu is one-item-per-row and scrollable in a full-height slide-out panel.
- Added overflow actions for `Kiosk Mode`, `Full Kiosk Mode`, and `Screen saver`.
- Implemented screensaver overlay behavior:
  - full-screen landscape image overlay,
  - runs with full-kiosk behavior,
  - double tap/double click unlock returns to the previous screen.
- Removed ambient view legacy focus-mode controls/text and removed its old screensaver dock button (screensaver now controlled from overflow menu).
- Updated Lovelace resource cache-bust:
  - `/local/nx-displaygrid/nx-displaygrid.js?v=20260226-151700`

### Why it changed
- To match the requested family-dashboard interaction model and improve stability/clarity on small screens without desktop overflow clutter.

### How to verify
1. Run checks:
   - `bash scripts/check.sh`
   - `bash scripts/check_structure.sh`
2. Hard refresh browser after deploy (cache-bust updated).
3. In schedule/important/chores top chips:
   - confirm no role icons or count metrics,
   - confirm names are larger, left-aligned, ellipsized,
   - confirm each row uses equal widths and each chip never exceeds 35% width,
   - confirm all chips are active after hard refresh.
4. In family dashboard:
   - confirm header title shows `Family Dashboard` (or configured title),
   - change title in Settings -> Sources & Dashboard -> Family dashboard title and save; confirm header updates.
5. In Settings:
   - change `People chips per row`, save, and confirm chip row layout updates.
6. On compact/mobile width:
   - confirm 3-dot menu appears, is scrollable full-height panel, and shows one item per row.
7. On desktop width:
   - confirm 3-dot mobile menu is not shown.
8. In compact/mobile 3-dot menu:
   - toggle `Kiosk Mode` on/off and confirm dashboard sidebar hides/shows.
   - toggle `Full Kiosk Mode` on/off and confirm full-screen overlay behavior toggles.
   - start `Screen saver`; confirm full-screen image overlay appears and double tap/double click exits back to previous screen.

### Version assumptions
- Verified against Home Assistant frontend docs and current 2026.2 release notes that Lovelace custom resource loading/resource mode behavior used by `/local/...` modules remains unchanged for this patch.
