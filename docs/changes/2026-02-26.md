# 2026-02-26

## Food favourites + chip layout polish

### What changed
- Food view navigation now includes a dedicated `Favourites` tab.
- Added favourites panel toggle at the top of the panel:
  - `Favourite meals` (recipes with average rating `>= 3`).
  - `Favourite shopping` (items starred in shopping).
- Improved Food weekly meal-plan row responsiveness:
  - Per-day actions (`Add to shopping`, `Begin cooking`) are grouped in a responsive action block.
  - Layout now reflows cleanly on narrower widths to avoid squashed controls.
- Split shopping concerns in Food:
  - `Shopping List` remains separate.
  - Saved shopping-list bundles are rendered in shopping context instead of crowding the meal-plan panel.
- Updated topbar person chips (used in schedule/important/chores contexts):
  - Fixed 4 equal-width chips per row.
  - Counts moved below the name in a stacked layout.
  - Long names now clip with ellipsis to avoid chip distortion.
  - On mobile widths, chips switch to circular avatar mode with bold first-letter initials while preserving active-toggle highlight behavior.
  - Duplicate initials are now disambiguated on mobile (uses two-letter badges when needed).
- Shopping star consistency fix:
  - Only explicitly starred shopping items are shown with active star state.
  - Common/history quick-add items no longer show as starred unless actually starred.
- Updated Lovelace resource cache-bust:
  - `/local/nx-displaygrid/nx-displaygrid.js?v=20260226-141200`

### Why it changed
- To stabilise layout on smaller screens and implement the requested favourites split/toggle behavior while keeping chip rows visually consistent with long names.

### How to verify
1. Run checks:
   - `bash scripts/check.sh`
   - `bash scripts/check_structure.sh`
2. Hard refresh the browser after deploy (cache-bust updated).
3. In Food view:
   - Confirm tabs include `Meals`, `Recipes`, `Shopping List`, `Favourites` (and `Cooking` only when active).
   - In `Favourites`, switch between `Favourite meals` and `Favourite shopping` using the top toggle.
   - Confirm favourite meals list only includes recipes with average rating >= 3.
   - Confirm favourite shopping list shows starred shopping entries and can add/unstar.
4. In Meals view, reduce viewport width and confirm day rows/actions no longer collapse into unusable narrow buttons.
5. In schedule/chores/important top chips:
   - Confirm 4 chips per row with equal width.
   - Confirm counts are displayed beneath each name.
   - Confirm long names are ellipsized and no chip overlap/squash occurs.
   - On a narrow/mobile viewport, confirm chips become circles with initial letters and active toggles remain visually highlighted.
   - If two names share the same first letter, confirm badges are disambiguated (e.g. `AL`, `AN`).
6. In Shopping view:
   - Confirm only truly starred items show filled/active stars.
   - Confirm common/history items without starring show outline stars.

## Food shopping tab favourites cleanup

### What changed
- Added a `hideFavourites` option to `fb-shopping-view`.
- Food `Shopping List` now embeds shopping view with `hideFavourites=true`, so only the shopping list panel is shown there.
- Standalone `Shopping` screen remains unchanged and still shows the favourites panel.
- Updated Lovelace resource cache-bust:
  - `/local/nx-displaygrid/nx-displaygrid.js?v=20260226-105122`

### Why it changed
- To remove duplicated favourites UI from the Food `Shopping List` tab now that favourites are managed in the dedicated Food `Favourites` tab.

### How to verify
1. Run checks:
   - `bash scripts/check.sh`
   - `bash scripts/check_structure.sh`
2. Hard refresh the browser after deploy (cache-bust updated).
3. In Food -> `Shopping List`:
   - Confirm only `Saved Shopping Lists` and the shopping list panel are visible.
   - Confirm there is no side `Favourites` panel in this tab.
4. In standalone `Shopping` screen:
   - Confirm the right-side `Favourites` panel is still present and functional.

## Topbar chip cleanup

### What changed
- Removed event/chore count metrics from topbar people chips in schedule/important/chores.
- Updated chip layout to left-align names on desktop/tablet.
- Updated hover title text to show only the person name (no count summary).
- Updated Lovelace resource cache-bust:
  - `/local/nx-displaygrid/nx-displaygrid.js?v=20260226-105621`

### Why it changed
- To simplify the chip design and match the requested cleaner look.

### How to verify
1. Run checks:
   - `bash scripts/check.sh`
   - `bash scripts/check_structure.sh`
2. Hard refresh the browser after deploy (cache-bust updated).
3. In `Schedule`, `Chores`, and `Important` views:
   - Confirm person chips show only color dot/icon + name (no numeric counts).
   - Confirm person names are left-aligned in each chip.

## Food favourites shopping list parity

### What changed
- Updated Food `Favourites` -> `Favourite shopping` to render the same deduplicated combined list used by Shopping tab favourites (`shoppingFavourites + shoppingCommon`).
- Added per-row star action label switching (`Star`/`Unstar`) based on current starred state.
- Updated section hint text to reflect starred plus common quick-add entries.
- Updated Lovelace resource cache-bust:
  - `/local/nx-displaygrid/nx-displaygrid.js?v=20260226-113519`

### Why it changed
- To keep the moved Food favourites shopping section consistent with the list users previously saw in the Shopping tab favourites panel.

### How to verify
1. Run checks:
   - `bash scripts/check.sh`
   - `bash scripts/check_structure.sh`
2. Hard refresh the browser after deploy (cache-bust updated).
3. In Food -> `Favourites` -> `Favourite shopping`:
   - Confirm previously visible shopping quick-add entries are listed.
   - Confirm each row can be added to shopping and can be starred/unstarred.

## Food favourites shopping styling restore

### What changed
- Restyled Food `Favourites` -> `Favourite shopping` rows to match the prior shopping-favourites visual pattern:
  - bordered pill row,
  - inline green `+` quick-add marker,
  - right-side star icon with filled/outline state.
- Row click/keyboard Enter+Space adds the item to shopping; star icon toggles favourite state without triggering add.
- Updated Lovelace resource cache-bust:
  - `/local/nx-displaygrid/nx-displaygrid.js?v=20260226-120816`

### Why it changed
- To bring back the previous, denser favourites-row styling that was easier to scan than the interim text-button layout.

### How to verify
1. Run checks:
   - `bash scripts/check.sh`
   - `bash scripts/check_structure.sh`
2. Hard refresh the browser after deploy (cache-bust updated).
3. In Food -> `Favourites` -> `Favourite shopping`:
   - Confirm each item renders as a compact pill row with inline `+` and star icon.
   - Confirm clicking row body adds the item to shopping.
   - Confirm clicking star toggles between filled and outline without adding the item.

## One-terminal sync/restart/log helper

### What changed
- Added executable helper script:
  - `deploy/rsync-restart-logcheck.sh`
- The script stages env config (`lab`/`live`), runs `rsync`, restarts HA Core, waits (default 300s), then fetches and scans log output for warning/error patterns.
- Added usage docs in `README.md` under deploy workflow.

### Why it changed
- To remove the need for two terminals when doing routine deploy + restart + post-restart log checks.

### How to verify
1. Run:
   - `deploy/rsync-restart-logcheck.sh --help`
2. Dry-run usage pattern with your host:
   - `deploy/rsync-restart-logcheck.sh 100.77.2.5 live`
3. Confirm output shows:
   - rsync step,
   - restart step,
   - wait step,
   - fetched logs,
   - warning/error summary line.

## Family dashboard kiosk/chips + mobile menu cleanup

### What changed
- Updated people chips in topbar (schedule/important/chores):
  - removed kid/grownup role icons,
  - increased name prominence and left-aligned names,
  - added configurable columns via `family_dashboard_v3.people_chips_per_row` (default `5`) with capped per-chip width (`35%`),
  - kept compact/mobile circular initial-chip mode with active-state highlight.
- Set person chip filters to default to “all selected” after hard refresh/first load.
- Added family dashboard header title config (`family_dashboard_v3.title`) and wired family header to use it (default `Family Dashboard`).
- Fixed date-next/date-prev/today/date-set adaptive bounce by locking adaptive auto-screen after manual date navigation; added regression test.
- Reworked compact overflow (3-dot) behavior:
  - now shown only in compact/small-screen mode,
  - menu is one-item-per-row and scrollable in a full-height slide-out panel.
- Added overflow actions for `Kiosk Mode`, `Full Kiosk Mode`, and `Screen saver`.
- Implemented screensaver overlay behavior:
  - full-screen landscape image overlay,
  - runs with full-kiosk behavior,
  - double tap/double click unlock returns to the previous screen.
- Removed ambient view legacy focus-mode controls/text and removed its old screensaver dock button (screensaver now controlled from overflow menu).
- Updated Lovelace resource cache-bust:
  - `/local/nx-displaygrid/nx-displaygrid.js?v=20260226-151700`

### Why it changed
- To match the requested family-dashboard interaction model and improve stability/clarity on small screens without desktop overflow clutter.

### How to verify
1. Run checks:
   - `bash scripts/check.sh`
   - `bash scripts/check_structure.sh`
2. Hard refresh browser after deploy (cache-bust updated).
3. In schedule/important/chores top chips:
   - confirm no role icons or count metrics,
   - confirm names are larger, left-aligned, ellipsized,
   - confirm each row uses equal widths and each chip never exceeds 35% width,
   - confirm all chips are active after hard refresh.
4. In family dashboard:
   - confirm header title shows `Family Dashboard` (or configured title),
   - change title in Settings -> Sources & Dashboard -> Family dashboard title and save; confirm header updates.
5. In Settings:
   - change `People chips per row`, save, and confirm chip row layout updates.
6. On compact/mobile width:
   - confirm 3-dot menu appears, is scrollable full-height panel, and shows one item per row.
7. On desktop width:
   - confirm 3-dot mobile menu is not shown.
8. In compact/mobile 3-dot menu:
   - toggle `Kiosk Mode` on/off and confirm dashboard sidebar hides/shows.
   - toggle `Full Kiosk Mode` on/off and confirm full-screen overlay behavior toggles.
   - start `Screen saver`; confirm full-screen image overlay appears and double tap/double click exits back to previous screen.

### Version assumptions
- Verified against Home Assistant frontend docs and current 2026.2 release notes that Lovelace custom resource loading/resource mode behavior used by `/local/...` modules remains unchanged for this patch.

## Family dashboard follow-up polish (menus, favourites qty, date-based meal plan)

### What changed
- Restored always-visible topbar 3-dot menu on desktop/tablet/mobile headers and kept menu items as single-row left-aligned entries.
- Added ambient bottom-right circular 3-dot menu button beside Heating/Lighting controls with matching actions (navigation, sync, kiosk/full-kiosk/screensaver).
- Updated Family `Open Important` modal to include due chores for today and tomorrow alongside events.
- Updated Family summary cards:
  - `House issues` renamed to `Home alerts`.
  - Removed duplicate lower `Chores` quick action so chores is only shown in the top summary stat area.
  - House mode quick action now routes to Home Controls (`home` view).
- Enabled `home` in family-mode allowed views to support the house-mode navigation target.
- Updated chores person toggles to support up to 2 active people columns; selecting a 3rd person automatically deselects the oldest active selection.
- Enhanced shopping favourites:
  - favourites now support persisted quantity metadata,
  - favourites add actions respect stored quantity,
  - added favourite edit (name + quantity) actions in shopping/favourites views.
- Food updates:
  - `Favourites` now defaults to `Favourite shopping`,
  - favourite shopping rows display quantity and include edit action,
  - recipe ingredient draft defaults to qty `1` and no quantity type selected,
  - recipe save now shows validation feedback when ingredient/step text is typed but not added via `+`.
- Meal plan is now week/date based:
  - added prev/today/next week controls,
  - stores week-specific menu assignments in `food_v2.menu_weeks`,
  - supports planning future weeks while preserving legacy current-week menu compatibility.
- Added device-scoped theme options in Settings:
  - `Current`, `Pale`, `Dark mode`, `Crystal glass`.
- Updated Lovelace resource cache-bust:
  - `/local/nx-displaygrid/nx-displaygrid.js?v=20260226-171900`

### Why it changed
- To align the post-review interaction details (menu visibility/access, favourites workflow, chores selection behavior, and future meal planning) with the intended daily usage flow across desktop and smaller screens.

### How to verify
1. Run checks:
   - `bash scripts/check.sh`
   - `bash scripts/check_structure.sh`
2. Hard refresh browser after deploy (cache-bust updated).
3. Header and menu:
   - confirm 3-dot button is visible in headers (including chores/family),
   - confirm menu items are one-row left-aligned and kiosk/full-kiosk/screensaver actions exist,
   - on mobile widths, confirm menu is a full-height scrollable panel.
4. Ambient:
   - confirm bottom-right circular 3-dot menu button appears beside Heating/Lighting and opens the same action menu set.
5. Family:
   - confirm `Open Important` modal includes both events and due chores for today/tomorrow,
   - confirm summary shows `Home alerts`,
   - confirm lower quick-action area no longer includes duplicate `Chores`.
6. House mode:
   - click House mode action/card and confirm it opens Home Controls page.
7. Chores view:
   - toggle person chips and confirm max 2 active columns; selecting a 3rd deselects the first selected.
8. Food favourites:
   - open Food -> Favourites and confirm `Favourite shopping` is selected by default,
   - edit a favourite item and quantity, then add to shopping and confirm quantity is applied.
9. Meal planning:
   - in Food -> Meals, use prev/next week controls, assign meals in future week, navigate away/back, and confirm entries persist by week.
10. Recipe draft validation:
   - type ingredient/step text but do not click `+`, click `Add recipe`, and confirm inputs are highlighted with validation guidance.

### Version assumptions
- Assumed Home Assistant 2026.2+ frontend behavior for custom module cache-bust handling and no schema conflicts for nested `food_v2.menu_weeks` object persistence in existing config storage flow.

## Settings tabs + notification and shopping quantity follow-up

### What changed
- Reworked Settings navigation into main tabs with subtabs:
  - `Settings`: `Sources`, `People`, `Family Panels`, `Value Config`, `Home Controls`, `Bins`.
  - `Preferences`: `Health & Drift`, `Styles & Config`, `Reminders`.
  - `Admin`: `Admin`, `Audit`.
- Moved audit access under the new `Admin` tab sub-navigation.
- Moved admin PIN/device-access controls from main settings content into the `Admin` tab.
- Removed the old Settings-side `Shopping favourites` controls.
- Updated `Heating entities` / `Lighting entities` config controls to multi-select from entities currently configured in `Home controls`.
- Updated notification policy settings:
  - `Phone notifications` defaults to on unless explicitly disabled.
  - `Notify services` now uses a multi-select list from available Home Assistant `notify.*` services.
- Updated notification sending logic to support multiple notify services.
- Added a shopping-list quantity-type selector per item (configured food units + `quantity` fallback).
- Updated favourite-shopping rows to always show quantity labels (including `x1`).
- Reduced refresh-time screen flips by removing forced adaptive auto-screen re-evaluation at refresh completion.
- Updated Lovelace cache-bust:
  - `/local/nx-displaygrid/nx-displaygrid.js?v=20260226-190200`

### Why it changed
- To align the settings layout with the requested tab model, remove text-entry friction for service/entity selection, and complete quantity-aware shopping controls while reducing unexpected refresh navigation jumps.

### How to verify
1. Run checks:
   - `bash scripts/check.sh`
   - `bash scripts/check_structure.sh`
2. Hard refresh browser after deploy (cache-bust updated).
3. Open `Settings` and confirm main tabs + subtab rows match the requested structure.
4. In `Settings -> Home Controls`, confirm `Heating entities` and `Lighting entities` are multi-select lists populated from configured Home controls.
5. In `Preferences` notification policy, confirm:
   - `Phone notifications` defaults to `On`.
   - `Notify services` offers multi-select `notify.*` options.
6. In `Shopping` view, confirm each item row has quantity +/- and a quantity-type dropdown.
7. In shopping/food favourites lists, confirm quantities are shown for all rows (including `x1`).
8. Leave a non-calendar screen open through an interval refresh and confirm it no longer force-jumps immediately to schedule on refresh completion.

### Version assumptions
- Assumed Home Assistant 2026.2+ custom-resource/frontend behavior remains stable for `/local/...` module cache-bust loading and `notify` service discovery via `hass.services`.

## Cooking progress refresh-loss fix

### What changed
- Added a user/device-scoped local cooking snapshot for Food cooking sessions (`nx-displaygrid:food-cooking:<userId>`).
- Cooking lifecycle now syncs snapshot state immediately:
  - `Begin cooking` writes snapshot,
  - each step checkbox toggle updates snapshot with `updatedAt`,
  - `Finish cooking` and recipe removal clear snapshot.
- Food data resolution now recovers cooking state from local snapshot when shared config is stale/missing and prefers the newer active state by timestamp.
- Updated Lovelace cache-bust:
  - `/local/nx-displaygrid/nx-displaygrid.js?v=20260226-191800`

### Why it changed
- To prevent in-progress cooking checklist state from disappearing after refresh/reload or transient backend config lag.

### How to verify
1. Run checks:
   - `bash scripts/check.sh`
   - `bash scripts/check_structure.sh`
2. Hard refresh browser after deploy (cache-bust updated).
3. In Food -> Meals:
   - start cooking for a day/meal,
   - tick multiple steps in Cooking tab.
4. Trigger refresh/reload:
   - wait for interval refresh and/or manually reload browser.
5. Confirm:
   - Cooking tab remains active for that meal,
   - previously checked steps are still checked,
   - finish cooking clears the cooking tab/session.

### Version assumptions
- Assumed Home Assistant 2026.2+ frontend behavior for localStorage persistence and unchanged config patch APIs used by `food_v2` updates.

## Recipe composer draft persistence + clear action

### What changed
- Added per-user/device local draft persistence for Food recipe composer inputs:
  - recipe name,
  - added ingredient list,
  - added step list,
  - pending ingredient input fields,
  - pending step input field,
  - edit target recipe id.
- Recipe composer draft now hydrates automatically when returning to Food view, so navigating to other views and back no longer drops in-progress recipe work.
- Added `Clear draft` button beside `Add recipe` / `Save recipe` in Food -> Recipes.
- Updated Lovelace cache-bust:
  - `/local/nx-displaygrid/nx-displaygrid.js?v=20260226-194100`

### Why it changed
- To prevent losing partially entered recipe content during view changes and refresh/re-render cycles, while still allowing explicit reset when starting over.

### How to verify
1. Run checks:
   - `bash scripts/check.sh`
   - `bash scripts/check_structure.sh`
2. Hard refresh browser after deploy (cache-bust updated).
3. In Food -> Recipes:
   - type a recipe name,
   - add several ingredients/steps and leave some pending input text.
4. Navigate to another view (e.g. Schedule) and return to Food -> Recipes.
5. Confirm all draft inputs/lists are still present.
6. Wait for an interval refresh (or trigger manual refresh) and confirm the draft remains.
7. Click `Clear draft` and confirm all recipe composer fields reset.

### Version assumptions
- Assumed Home Assistant 2026.2+ frontend behavior for localStorage persistence and unchanged custom-card lifecycle semantics when switching views.

## Recipe step inline edit and reordering

### What changed
- In Food -> Recipes composer, added inline step editing for already-added steps.
- Added step order controls while composing:
  - `Up`
  - `Down`
- Step edit/reorder/remove operations now update persisted recipe draft state.
- Saved recipe list action label updated from `Edit` to `Edit recipe` to make post-save editing intent explicit.
- Updated Lovelace cache-bust:
  - `/local/nx-displaygrid/nx-displaygrid.js?v=20260226-194900`

### Why it changed
- To support editing and reorganizing step order during recipe creation and make editing saved recipes clearer.

### How to verify
1. Run checks:
   - `bash scripts/check.sh`
   - `bash scripts/check_structure.sh`
2. Hard refresh browser after deploy (cache-bust updated).
3. In Food -> Recipes:
   - add multiple steps with `+`,
   - edit step text inline,
   - move steps up/down,
   - remove a step,
   - save recipe and confirm order/text persist.
4. In saved recipes list:
   - click `Edit recipe`,
   - confirm existing steps load into the composer,
   - edit/reorder and save,
   - confirm saved recipe reflects updated step order/content.

### Version assumptions
- Assumed Home Assistant 2026.2+ frontend lifecycle behavior for Lit custom elements and unchanged card config update semantics.

## Quantity-type sources, default `x`, and settings save visibility

### What changed
- Quantity-type dropdown options now come only from `food_v2.units` configured in Settings:
  - removed built-in fallback defaults from `_foodUnitOptions()`,
  - recipe ingredient unit dropdown no longer injects a fallback `quantity` option.
- Shopping item unit dropdown now uses only configured quantity types plus a blank option (`x`), not ad-hoc per-item unit additions.
- Quantity formatting is now consistent as `${amount}${quantityType} ${item}`:
  - recipe ingredient display now shows entries like `1x banana` and `1g chicken`,
  - recipe-to-shopping conversion now emits the same format,
  - shopping text formatter now writes no-unit entries as `x` quantity type by default.
- Settings top tab row now keeps save controls visible with long sub-tab pill sets:
  - added a dedicated `saveControls` container,
  - allowed sub-tab pill row horizontal overflow handling,
  - adjusted mobile placement so save controls move to a full-width row.
- Updated Lovelace cache-bust:
  - `/local/nx-displaygrid/nx-displaygrid.js?v=20260226-210300`

### Why it changed
- To enforce a single source of truth for quantity types from Settings, use `x` as the no-unit default, keep ingredient text ordering consistent, and prevent `Save changes` from being pushed off-screen in Settings -> Value Config.

### How to verify
1. Run checks:
   - `bash scripts/check.sh`
   - `bash scripts/check_structure.sh`
2. Hard refresh browser after deploy (cache-bust updated).
3. In Settings -> Value Config:
   - set units to a custom set (for example: `g, kg, ml`),
   - confirm `Save changes` remains visible and clickable.
4. In Food -> Recipes:
   - open ingredient quantity type dropdown,
   - confirm only configured units appear (plus `x`),
   - add ingredients and confirm display format like `1g chicken` or `1x banana`.
5. In Food -> Shopping:
   - open an item unit dropdown,
   - confirm only configured units appear (plus `x`),
   - confirm no-unit items are rendered as `1x item-name`.

### Version assumptions
- Assumed Home Assistant 2026.2+ frontend behavior for CSS sticky/flex layouts and unchanged Lit render tree structure in this custom card.

## Recipe layout polish + active-screen retention

### What changed
- Food -> Recipes ingredient draft inputs now render in this order:
  - amount
  - quantity type
  - item name
  - add button (`+`)
- Saved recipe cards were restructured:
  - recipe title now sits on its own top row,
  - removed compressed ingredient summary text line,
  - added collapsible `Ingredients` and `Steps` sections,
  - each ingredient/step is rendered on its own row.
- Added interaction-based adaptive lock refresh:
  - pointer, touch, keyboard, and input activity now refresh the manual interaction timestamp used by adaptive auto-screen logic.
  - This prevents periodic refresh/adaptive ticks from kicking users back to landing/calendar while they are actively interacting in-place.
- Updated Lovelace cache-bust:
  - `/local/nx-displaygrid/nx-displaygrid.js?v=20260226-214600`

### Why it changed
- To match requested recipe editor/list layout and stop disruptive in-session screen jumps during active work.

### How to verify
1. Run checks:
   - `bash scripts/check.sh`
   - `bash scripts/check_structure.sh`
2. Hard refresh browser after deploy (cache-bust updated).
3. In Food -> Recipes (left composer panel):
   - confirm ingredient add row order is amount -> quantity type -> item name -> `+`.
4. In Food -> Recipes (saved recipes panel):
   - confirm recipe title is on its own row at top,
   - confirm no compressed ingredient summary line is shown,
   - confirm `Ingredients` and `Steps` are collapsible and each line item is one row.
5. Navigate to a non-schedule screen (for example `Food`), then actively type/click in the UI while background refreshes occur:
   - confirm the view no longer snaps back to landing/calendar during active interaction.

### Version assumptions
- Assumed Home Assistant 2026.2+ frontend event-dispatch behavior for pointer/keyboard/input events on custom card hosts and unchanged adaptive auto-screen feature flags.

## Quantity label cleanup + collapsed recipe sections

### What changed
- Removed `default` wording from blank quantity-type options:
  - Food recipe ingredient quantity type now shows `x`,
  - Shopping quantity type now shows `x`.
- Saved recipe card collapsible sections now start closed by default:
  - `Ingredients`,
  - `Steps`.
- Updated Lovelace cache-bust:
  - `/local/nx-displaygrid/nx-displaygrid.js?v=20260226-220800`

### Why it changed
- To simplify the quantity label text and keep saved recipe cards less expanded/noisy by default.

### How to verify
1. Run checks:
   - `bash scripts/check.sh`
   - `bash scripts/check_structure.sh`
2. Hard refresh browser after deploy (cache-bust updated).
3. In Food -> Recipes:
   - confirm ingredient quantity type blank option label reads `x`.
4. In Food -> Shopping:
   - confirm item quantity type blank option label reads `x`.
5. In saved recipes list:
   - open a recipe card and confirm both `Ingredients` and `Steps` are collapsed on initial render.

### Version assumptions
- Assumed Home Assistant 2026.2+ frontend `<details>/<summary>` behavior and unchanged Lit rendering for saved recipe cards.

## Recipe ingredient edit mode

### What changed
- Food -> Recipes composer ingredient rows now include an edit (`pencil`) action.
- Clicking edit opens inline fields for that ingredient row:
  - amount,
  - quantity type,
  - item name.
- Edit mode supports:
  - `Save` to apply updates,
  - `Cancel` to discard row edits,
  - `Remove` while editing.
- Updated Lovelace cache-bust:
  - `/local/nx-displaygrid/nx-displaygrid.js?v=20260226-223900`

### Why it changed
- To allow editing existing ingredient rows during both new recipe creation and editing saved recipes.

### How to verify
1. Run checks:
   - `bash scripts/check.sh`
   - `bash scripts/check_structure.sh`
2. Hard refresh browser after deploy (cache-bust updated).
3. In Food -> Recipes, add one or more ingredients.
4. Click the pencil on an ingredient row and verify amount/type/name fields appear inline.
5. Update values and click `Save`; confirm the row text updates.
6. Repeat and click `Cancel`; confirm row remains unchanged.
7. Open an existing saved recipe via `Edit recipe` and repeat steps 4-6.

### Version assumptions
- Assumed Home Assistant 2026.2+ frontend event behavior for Lit inline button/input handlers and unchanged `food_v2` payload structure.

## Blank quantity type behavior in Food + Shopping

### What changed
- Updated quantity-type dropdown labels in Food recipe ingredient add/edit and Shopping item unit selectors:
  - replaced `x` with `blank`,
  - `blank` option uses a single-space value for recipe ingredient unit persistence.
- Updated ingredient/shopping text formatting:
  - blank quantity type is hidden in rendered list text,
  - when blank quantity type is paired with amount `1`, the amount is hidden too (shows only item name),
  - when blank quantity type is paired with amount greater than `1`, output is `amount item`.
- Updated recipe ingredient unit normalisation paths to preserve blank as a single space through draft/save/edit flows.
- Updated Lovelace cache-bust:
  - `/local/nx-displaygrid/nx-displaygrid.js?v=20260226-231500`

### Why it changed
- To support an explicit no-quantity-type option that keeps list text clean and avoids showing redundant `1` values.

### How to verify
1. Run checks:
   - `bash scripts/check.sh`
   - `bash scripts/check_structure.sh`
2. Hard refresh browser after deploy (cache-bust updated).
3. In Food -> Recipes:
   - open ingredient quantity-type dropdown and confirm `blank` option exists,
   - add `1` + `blank` + `banana` and confirm ingredient list renders `banana`,
   - add `2` + `blank` + `banana` and confirm ingredient list renders `2 banana`.
4. Save/edit a recipe with blank quantity type and reload:
   - confirm blank selection persists and still renders without a quantity type label.
5. In Shopping:
   - set an item unit to `blank` and confirm list text hides quantity type,
   - confirm quantity `1` shows only item name.

### Version assumptions
- Assumed Home Assistant 2026.2+ custom-card frontend behavior for Lit select bindings and unchanged `food_v2` recipe ingredient schema.

## Quantity type split: `x` vs `blank`

### What changed
- Updated Food recipe and Shopping quantity/unit handling so `x` and `blank` are treated as distinct options:
  - `x` is now explicitly available and remains visible in rendered text,
  - `blank` remains available and renders with no quantity type text.
- Updated Food recipe quantity dropdowns:
  - include both `blank` and `x`,
  - default draft selection now uses `x`,
  - custom units from settings are still included (excluding duplicate `x`).
- Updated Shopping unit dropdown:
  - include both `x` and `blank`,
  - preserve `x` as a visible canonical unit.
- Updated rendering rules:
  - `blank` + amount `1` => item name only,
  - `blank` + amount `>1` => amount + item name (no type),
  - `x` remains visible (for example `1x banana`).
- Updated Lovelace cache-bust:
  - `/local/nx-displaygrid/nx-displaygrid.js?v=20260226-234500`

### Why it changed
- To match requested behavior where `x` is explicitly visible and `blank` is explicitly invisible, with special-casing only for amount `1` when `blank` is selected.

### How to verify
1. Run checks:
   - `bash scripts/check.sh`
   - `bash scripts/check_structure.sh`
2. Hard refresh browser after deploy (cache-bust updated).
3. In Food -> Recipes:
   - confirm quantity type dropdown includes both `x` and `blank`,
   - add `1 + blank + banana` and confirm output is `banana`,
   - add `2 + blank + banana` and confirm output is `2 banana`,
   - add `1 + x + banana` and confirm output is `1x banana`.
4. In Shopping:
   - confirm unit dropdown includes both `x` and `blank`,
   - set an item to `x` and confirm rendered output keeps `x` visible,
   - set an item to `blank` and confirm quantity type is hidden (and quantity `1` hidden too).

### Version assumptions
- Assumed Home Assistant 2026.2+ frontend select binding behavior and unchanged shopping text parser/serializer wiring in this custom card.

## Recipe embedding into recipe drafts

### What changed
- Added recipe embedding flow in Food -> Recipes:
  - each saved recipe card now has an `Embed in draft` action.
- Embedding behavior:
  - appends the selected recipe's ingredients into the current draft ingredients list,
  - appends the selected recipe's steps into the current draft steps list,
  - keeps the current draft in-place and persists it via existing draft storage.
- Added safety checks:
  - blocks embedding a recipe into itself while editing that same recipe,
  - shows an error when a source recipe has no ingredients and no steps.
- Updated Lovelace cache-bust:
  - `/local/nx-displaygrid/nx-displaygrid.js?v=20260226-235800`

### Why it changed
- To support composing larger recipes from existing recipes without retyping ingredient and step lists.

### How to verify
1. Run checks:
   - `bash scripts/check.sh`
   - `bash scripts/check_structure.sh`
2. Hard refresh browser after deploy (cache-bust updated).
3. Open Food -> Recipes and start a new draft (or edit a draft in progress).
4. In the saved recipes panel, click `Embed in draft` on any recipe:
   - confirm ingredients and steps are appended into the draft editor,
   - confirm the toast indicates recipe was embedded.
5. Open an existing recipe in edit mode and click `Embed in draft` on that same recipe card:
   - confirm action is disabled and/or self-embed is rejected.

### Version assumptions
- Assumed Home Assistant 2026.2+ Lit rendering/event behavior and unchanged `food_v2` recipe schema for ingredient/step arrays.
